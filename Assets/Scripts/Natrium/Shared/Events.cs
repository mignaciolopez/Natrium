namespace Natrium.Shared
{
    public enum Events
    {
        OnConnected,
        OnDisconnected,

        OnPrimaryClick,
        OnKeyCodeReturn,
        OnKeyCodeEscape,

        OnSendPing
    }
}
